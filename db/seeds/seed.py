"""
This file should contain everything to seed the database with default values.
The data can then be loaded with

(venv) $ python manage.py seed <seed file name>

Examples:

   from extensions import db
   user = User(name='Bob', is_admin=False)
   db.session.add(user)
   db.session.commit()

"""
